diff --git a/node_modules/@odion-cloud/capacitor-volume-control/OdionCloudCapacitorVolumeControl.podspec b/node_modules/@odion-cloud/capacitor-volume-control/OdionCloudCapacitorVolumeControl.podspec
new file mode 100644
index 0000000..c087b22
--- /dev/null
+++ b/node_modules/@odion-cloud/capacitor-volume-control/OdionCloudCapacitorVolumeControl.podspec
@@ -0,0 +1,17 @@
+require 'json'
+
+package = JSON.parse(File.read(File.join(__dir__, 'package.json')))
+
+Pod::Spec.new do |s|
+  s.name = 'OdionCloudCapacitorVolumeControl'
+  s.version = package['version']
+  s.summary = package['description']
+  s.license = package['license']
+  s.homepage = package['repository']['url']
+  s.author = package['author']
+  s.source = { :git => package['repository']['url'], :tag => s.version.to_s }
+  s.source_files = 'ios/Plugin/**/*.{swift,h,m,c,cc,mm,cpp}'
+  s.ios.deployment_target  = '13.0'
+  s.dependency 'Capacitor'
+  s.swift_version = '5.1'
+end
\ No newline at end of file
