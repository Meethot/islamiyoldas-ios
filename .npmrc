# @odion-cloud/capacitor-volume-control@2.0.1 peer olarak @capacitor/core@^5 istiyor,
# proje Capacitor 8'de. Eklenti yalnızca AudioManager sarmalayıcısı, çalışıyor.
# Bu satır olmadan temiz bir `npm ci` / `npm install` ERESOLVE ile durur —
# node_modules silinmiş bir makinede (yeni klon, CI, release) kurulum hiç bitmez.
legacy-peer-deps=true
